// Placeholder — replaced by the real subjects list page in Task 5.
// Its only job right now is to give the `(teach)` route group something to
// render at `/teach` so the role-gated shell (TeachLayout + TeachSidebar) is
// reachable and testable.
export default function TeachPlaceholderPage() {
  return null;
}
